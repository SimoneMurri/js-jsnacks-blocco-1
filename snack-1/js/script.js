const firstAge = prompt('type your age');
const secondAge = prompt('type your age');

if (firstAge > secondAge){
   console.log(firstAge);
} else if (secondAge > firstAge) {
   console.log(secondAge);
}
   